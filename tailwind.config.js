/**
 * 🌬️ Tailwind CSS v4 Configuración
 * 
 * En versiones anteriores de Tailwind (v3), aquí se configuraban los colores,
 * fuentes y rutas. Pero con Tailwind v4, esta configuración se ha movido
 * directamente a los archivos CSS usando la directiva `@theme`.
 * 
 * Por eso este archivo está vacío. Si quieres cambiar colores o animaciones,
 * debes hacerlo en `src/styles.css`.
 */
module.exports = {};
